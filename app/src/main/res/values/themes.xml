<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/lightSand</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>

        <item name="postSplashScreenTheme">@style/Theme.Counter</item>

        <item name="android:windowBackground">@color/lightSand</item>
    </style>

    <style name="Theme.Counter" parent="Theme.Material3.DayNight.NoActionBar">
    </style>
</resources>