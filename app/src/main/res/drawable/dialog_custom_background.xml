<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Lo sfondo uguale ai tasti più e meno -->
    <solid android:color="@color/honeydew" />

    <!-- Il bordo con lo stesso spessore e colore dei tasti più e meno -->
    <stroke
        android:width="5dp"
        android:color="@color/deepSpaceBlue" />

    <!-- Arrotondiamo gli angoli del dialog per un look moderno -->
    <corners android:radius="16dp" />
</shape>